PORT=5001
JWT_SECRET=pizza_delivery_super_secret_key_123
# Leave MONGODB_URI empty to run MongoDB In-Memory database automatically!
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.wcs8qyo.mongodb.net/pizzaApp?retryWrites=true&w=majority
# Optional SMTP Configurations (Ethereal Mail will be auto-generated if left blank)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Pizza Delivery" <noreply@pizzadelivery.com>

# Razorpay Test Mode Credentials
# To use the official Razorpay checkout popup, add your real test keys from https://dashboard.razorpay.com
# Keys that start with rzp_test_ will open the real Razorpay modal.
# If left as dummy values, orders will use mock (silent) payment flow.
RAZORPAY_KEY_ID=rzp_test_dummykey123
RAZORPAY_KEY_SECRET=dummysecret123
